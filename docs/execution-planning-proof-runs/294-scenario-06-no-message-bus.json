{
  "scenario": "6. No message bus",
  "issue_294_text": "orchestrator dispatches workers from references and compact state without ingesting their instruction bodies or forwarding prose handoffs",
  "status": "demonstrated",
  "representative_real_work": "issue #294 itself -- this very worker unit's (294-D's) own dispatch reference, rendered live by the real format-unit-dispatch-prompt.mjs.",
  "exact_command": "node tools/orchestration/format-unit-dispatch-prompt.mjs --execution-issue 294 --unit 294-D",
  "exit_code": 0,
  "observed_output": "Worker-unit dispatch. Parent execution Issue: #294. Worker Unit Contract: https://github.com/LouPineWays/Loop-Dee-Loup/issues/294#issuecomment-5550653317. Shared Contract: https://github.com/LouPineWays/Loop-Dee-Loup/issues/294#issuecomment-5550652308.\n\nRead both comments directly from GitHub -- outcome, capability, files, verification, and State were not restated here on purpose. Execute per AGENTS.md (and docs/bounded-review-cycle.md if review-worthy), report via AGENTS.md's Slice handoff format, and update only this unit's own State field when done, per the Shared Contract's edit-ownership rule.",
  "output_byte_length": 606,
  "interpretation": "Running the real, unmodified format-unit-dispatch-prompt.mjs against #294/294-D produces a prompt that carries only three references (the Worker Unit Contract comment URL, the parent execution Issue number, and the Shared Contract comment URL) plus a fixed fifty-word instruction to read them directly -- it contains none of 294-D's own actual fields (its 'Required bounded outcome', 'Files/surfaces expected to change', 'Verification required', or 'State' text, all of which are substantial paragraphs in the real comment). The script imports and reuses format-dispatch-prompt.mjs's assertReferenceOnly() guard, which enforces this is not merely true by construction of the fixed template string but also checked against a length threshold before being emitted. This is real, live, reproducible evidence that the orchestrator-facing dispatch surface stays reference-only and never ingests/retransmits a worker's actual instruction body -- the same shape this very unit (294-D) was itself dispatched under (a reference to comment 5550653317 plus the Shared Contract comment 5550652308, not a restatement of either).",
  "honest_caveat": "This proof-run's own dispatch prompt (the message that started this session) was composed by hand by the orchestrating session rather than literally piped through this exact script invocation -- but it carried the identical reference shape (comment IDs plus a pointer to read them directly, no restated fields), and this scenario's evidence requirement is that the mechanism CAN and DOES produce a correct reference-only prompt when actually run, which the live command above demonstrates directly and reproducibly."
}
