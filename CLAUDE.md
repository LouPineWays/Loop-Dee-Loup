# Claude Code instructions

`AGENTS.md` is the single shared operating contract for Claude Code and Codex in this repository. It is imported automatically below, so it is active from session start without a manual `Read`.

@AGENTS.md

Repository-local Claude skills live under `.claude/skills/`. Invoke only the skill relevant to the current control problem; do not run them all as startup ceremony.
