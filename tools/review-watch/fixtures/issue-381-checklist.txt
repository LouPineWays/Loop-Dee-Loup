1. Confirm `AGENTS.md`'s Session-execution "continue mechanically" paragraph (originally ending "...until CLEAN completion") now scopes mechanical continuation to one bounded transition toward the next durable breakpoint, and explicitly states that durable lifecycle/control state persists while the *reasoning context* advancing it does not, consistent with `docs/operating-model.md` § Watched lifecycle breakpoints.
2. Confirm `AGENTS.md`'s Context-cost boundaries paragraph (~line 196) now reads "five-breakpoint fresh-context model" (not "four-breakpoint fresh-worker model"), matching `docs/operating-model.md`'s "Across all five breakpoints" summary line in the same section — this is the exact defect this PR's Stage 1 review found and this commit fixed.
3. Confirm `AGENTS.md`'s bounded-review-cycle paragraph in Context-cost boundaries now scopes mechanical continuation to *within one authorized stage* (verify/fix/rerun) and ends the controller at each of the four in-cycle breakpoints it names (PR/Stage 1, Stage 1 finding to correction, Stage 2 trigger after merge, NOT CLEAN to correction), rather than the prior single "continue through ... merge, and the next audit" phrasing that implied one continuous reasoning context across the whole cycle.
4. Confirm `docs/operating-model.md` § Watched lifecycle breakpoints enumerates exactly five breakpoints (1. initial implementation -> first PR, 2. PR review/fix, 3. merge -> Stage 2 audit, 4. Stage 2 NOT CLEAN -> correction -> re-audit, 5. CLEAN / terminal blocked / founder interrupt), each stated as ending both the controller and the worker's reasoning context, not merely the worker's.
5. Confirm no other repository content (code, tooling, templates, other docs) was touched by this PR — the diff is confined to `AGENTS.md` and `docs/operating-model.md` prose.
6. Confirm `gh api repos/LouPineWays/Loop-Dee-Loup/actions/runs?head_sha=fd1baa68eefd9dabec69784fa1c5390e9fa31ae5` shows a completed, successful run of `Control-plane paths` (this PR touches `AGENTS.md` and `docs/*.md`, both control-plane paths per `docs/bounded-review-cycle.md`'s Entry check) — checked against the PR's frozen reviewed head, not the merge commit, since that workflow triggers only on `pull_request`.
