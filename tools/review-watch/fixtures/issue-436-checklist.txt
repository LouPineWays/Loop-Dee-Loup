1. Confirm `resolvePostMergeVerdict`'s `READY_TO_CLOSE` branch (`tools/orchestration/next-review-transition-gate.mjs`) chains `lifecycle-gate.mjs close-work-issue` before `close-audit` in `nextCommand` whenever `postAudit.workIssue` is a real number, and that `ACCEPTED_NO_WORK_ISSUE` keeps an audit-only `nextCommand` (no work issue exists to close).
2. Confirm `checkCloseWorkIssue` (`tools/review-watch/lifecycle-gate.mjs`) is idempotent — `ALREADY_TERMINAL` on an already-closed work issue with no close/comment attempted — closes-then-comments on an open one in that order, and a comment-post failure still reports `CLOSED` (never a failed exit), naming the comment error.
3. Confirm `resolvePostMergeVerdict`'s `OK` branch now routes `verdict: "CLEAN"` + `workIssueState: "CLOSED"` to `STAGE2_CLOSE_READY` with an audit-only `nextCommand` (never re-attempting to close an already-closed work issue), while the same verdict with `workIssueState: "OPEN"` still falls through to `NO_ACTION_YET` unchanged (a combination `checkPostAudit` itself should never produce, but the gate must not invent `STAGE2_CLOSE_READY` from `workIssueState` alone without a backed CLEAN verdict).
4. Confirm `checkReadyDispatch` (`tools/orchestration/ready-dispatch-gate.mjs`) classifies `AUDIT_ISSUE_DETECTED` before applying its own open-state guard: a directly-dispatched Audit Issue that is already `CLOSED` resolves to exit 9 / `AUDIT_ISSUE_DETECTED`, while an ordinary closed thin control Issue (non-audit-shaped body) is unaffected and still reports `NOT_READY`.
5. Confirm `defaultGhIssueList` (`tools/review-watch/lifecycle-gate.mjs`) now fetches via `gh api search/issues -X GET -f q="[Audit] in:title repo:<repo>" --paginate --slurp` rather than `gh issue list --limit 200`, and that `normalizeSearchIssuesPage` correctly maps the REST Search API's snake_case/lowercase-state item shape into the `{ number, title, body, state, createdAt }` shape `checkCloseAudit`'s candidate walk expects, filtering out pull requests that match the same search text.
6. Confirm the whitespace-tolerance fix (`consumer-sync-gate.mjs`'s `isCleanStage1Response`, `next-review-transition-gate.mjs`'s own duplicated `hasFindingsStage1Response`) correctly classifies a `body_excerpt` beginning with a leading newline before `"### 💡 Codex Review"` as findings-bearing, and does not broaden into any semantic adjudication of finding content beyond the two fixed preambles already recognized before this fix.
7. Confirm `docs/bounded-review-cycle.md` and `AGENTS.md`'s `STAGE2_CLOSE_READY`/`AUDIT_ISSUE_DETECTED` prose was updated to describe `close-work-issue` as mechanized, not a manual prose reminder, consistent with the actual code, and that `node tools/check-startup-budget.mjs` / `node tools/check-control-plane-paths.mjs` both report OK against the exact merge commit.
8. Confirm no regression in the surrounding `merge-ready`/`post-audit`/`checkCloseAudit` machinery unrelated to the five items above: `node --test tools/review-watch/ tools/orchestration/` passes in full against the exact merge commit.
9. Confirm this correction pass's decision to skip a second inline Stage 1 Codex round (control #408 / PR #435's own instruction) is a reasonable application of this repository's established "one inline round, no second same-PR invocation" practice — cite whether the new execution surface introduced (`checkCloseWorkIssue`'s `gh issue close`/`gh issue comment` `execFileSync` calls, and the rewritten search-API pagination) is adequately covered by the existing test suite, or whether it warrants founder judgment despite the #426/#423 precedent for skipping a second round.
