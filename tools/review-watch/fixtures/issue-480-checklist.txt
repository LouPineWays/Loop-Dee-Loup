1. Confirm `replaceVerdictField()` (tools/review-watch/lifecycle-gate.mjs) repairs a completely missing `### Verdict` heading by appending a fresh `### Verdict` section after the existing body content, and that the result round-trips through `parseStage2Verdict()` to the written value while every pre-existing section (e.g. `### Work issue`, `### Exact merge commit`) survives byte-for-byte.
2. Confirm `replaceVerdictField()` repairs an empty `### Verdict` field (heading present, no non-blank value line before the next heading or end of body) by inserting the value directly beneath the heading, preserving neighboring sections (e.g. a following `### Next authorized action` field) unchanged.
3. Confirm `checkRecordVerdict()` re-evaluates `findStage2ReportEvidence()` against the freshly-read audit issue body immediately before mutating it (not merely re-reading the body's own `Verdict` field) — i.e. that `RECORDED`, `ALREADY_RECORDED`, and `CONFLICTING_VERDICT` are all decided against a revalidated evidence verdict, not the evidence `checkPostAudit` computed a moment earlier — and that a race where a newer completed report with the opposite verdict lands before the mutation step results in the newer report's verdict being recorded, never the stale one.
4. Confirm `checkRecordVerdict()` still fails closed (exit 1, no mutation) when revalidation finds no completed report backing any verdict, rather than recording a verdict revalidation itself could not re-establish.
5. Confirm `docs/bounded-review-cycle.md`'s Verdict handling section accurately documents `record-verdict`'s full result contract, including every `checkPostAudit` state it passes through unchanged (e.g. `READY_TO_CLOSE`, `OK`, `PREMATURE_CLOSURE`) when promotion does not apply, matching the actual `checkRecordVerdict()` implementation.
6. Confirm the full regression suite passes at the exact merge commit: `node --test tools/review-watch tools/orchestration` (779 tests expected, 0 failures), plus `node tools/check-startup-budget.mjs` and `node tools/check-control-plane-paths.mjs` both report OK.
7. Confirm `gh api repos/LouPineWays/Loop-Dee-Loup/actions/runs?head_sha=911cac6d6bd56059020574ad4de5ef3f54d552cd` shows a completed, successful run of the `Control-plane paths` workflow (this PR touches `tools/review-watch/**` and `docs/*.md`, both control-plane paths) — checked against the PR's frozen reviewed head, not the merge commit, since that workflow triggers only on `pull_request`.
8. Confirm none of the three Stage 1 findings' fixes introduced a regression against #439's original acceptance behavior: a completed CLEAN/NOT CLEAN report over unsettled durable verdict state still promotes deterministically; no/incomplete/wrong-commit evidence still fails closed or remains pending; settled-verdict and premature-closure behavior are unchanged; and a rerun after a successful `RECORDED` still reaches the existing post-audit transition without duplicate mutation or comments.
