1. Confirm `formatDispatchPrompt` (tools/orchestration/format-dispatch-prompt.mjs) rejects a missing/`NaN`/negative/non-integral `controlIssue` or `executionIssue`, and rejects an empty/whitespace-only `route`, throwing rather than emitting a malformed reference.
2. Confirm the CLI's piped-stdin path refuses (exit 2) both a payload with `state` entirely absent and a payload with a non-`"READY_TO_DISPATCH"` state, in both cases printing to stderr and writing nothing to stdout.
3. Confirm the CLI's explicit-flags path fails closed (non-zero exit, empty stdout) for a non-numeric `--control-issue`/`--execution-issue` value (e.g. `--control-issue not-a-number`).
4. Confirm `formatDispatchPrompt({controlIssue:322, executionIssue:321, route:"implementation worker"})` produces a prompt containing `#321`, `#322`, and `implementation worker`, is well under the 700-char reference-only threshold (`assertReferenceOnly`), and is deterministic (same input → identical output).
5. Confirm the module's own header/usage comments no longer advertise the explicit-flags path as suitable for a legacy-unsplit routing worker, and instead state explicitly that this formatter is scoped to implementation-worker dispatch only.
6. Confirm `AGENTS.md`'s Session execution and Subagent dispatch sections, and `docs/operating-model.md`'s Two-plane Issue dispatch section, now instruct piping the gate's JSON output into `format-dispatch-prompt.mjs` and using its stdout verbatim, rather than hand-composing the dispatch prompt.
7. Confirm `docs/diagnostic-traces/615f2b4a-69eb-42f1-bef6-432a4e32f4dc.json` is privacy-minimal (no raw prompt/transcript text, only the same structural fields every other artifact in this directory carries) and that its `index.json` entry's `pre_dispatch_status` (`violation`) matches `classifyPreDispatch`'s actual output for that trace.
8. Run the full local test suite (`node --test` across every `tools/**/*.test.mjs`) against this merge commit and confirm 0 failures, including the 12 new tests in `format-dispatch-prompt.test.mjs`.
9. Confirm no `Fixes`/`Closes`/`Resolves #321` (or case variant) appears in the PR body or any commit message that landed on `main`.
10. Confirm `node tools/check-control-plane-paths.mjs` still exits 0 against this merge commit.
11. Confirm the `Control-plane paths` workflow completed successfully against this PR's frozen reviewed head SHA `82651b3c8026ba118bb3bbf22c1dee6a09d27670` (this PR touches `AGENTS.md`, `docs/*.md`, and `tools/orchestration/**`, all control-plane paths).
