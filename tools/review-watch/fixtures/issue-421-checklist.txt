1. Confirm `runPrepareDispatchManifest` (`tools/orchestration/prepare-dispatch-manifest.mjs`) returns `exitCode: 3, state: "REPLAN_REQUIRED"` and never calls `postImpl` when any manifest entry resolves to `route=REPLAN_REQUIRED`, for both the `--create` and `--comment-id` persistence paths, and that a manifest with no REPLAN_REQUIRED units is unaffected (still reaches `DISPATCH_MANIFEST_VERIFIED`).
2. Confirm `parseManifestPlanIndexUrl` (`tools/orchestration/ready-dispatch-gate.mjs`) resolves a duplicate `- **Plan index:**` bullet in a Dispatch Manifest comment body via last-occurrence (matching `parseControlBullet`'s established convention), not the first match, and that a manifest with a single unambiguous bullet is unaffected.
3. Confirm `parseCommentPermalinkIdentity` in both `ready-dispatch-gate.mjs` and its independent copy in `prepare-dispatch-manifest.mjs` returns an `origin` field, and that every identity-equality comparison site in both files (the manifest-pointer-vs-canonical check and the manifest-Plan-Index-backlink check in `ready-dispatch-gate.mjs`; the write/read manifest identity check, the Plan-Index-backlink check, and the reparsed-dispatch-manifest check in `prepare-dispatch-manifest.mjs`) includes `.origin !==` alongside the existing repo/issue/commentId comparisons — i.e. a foreign-host permalink sharing path+fragment with a real GitHub comment is rejected.
4. Confirm `verifyRoutedDispatchManifest` and `checkReadyDispatch` (`tools/orchestration/ready-dispatch-gate.mjs`) report `operationalError: true` / exit 1 (ERROR) — not exit 3 (NOT_READY) — when `parseExecutionPlanImpl` returns `exitCode: 1` (an operational failure) or when the manifest-comment read throws, while a genuinely malformed/missing durable-state case (e.g. Dispatch manifest pointer `none`, wrong-issue manifest) still correctly reports exit 3 (NOT_READY).
5. Confirm regression tests exist covering all four items above (`prepare-dispatch-manifest.test.mjs`, `ready-dispatch-gate.test.mjs`) and that `node --test tools/orchestration/*.test.mjs` passes in full at the merge commit.
6. Confirm the merged PR carries no GitHub auto-close reference to issue #419 (neither a PR-body/commit-message closing keyword nor a Development-sidebar link) — `gh pr view 420 --repo LouPineWays/Loop-Dee-Loup --json closingIssuesReferences` should be empty — and that issue #419 remains OPEN with its NOT CLEAN verdict intact after this merge.
7. Control-plane workflow check: confirm `gh api repos/LouPineWays/Loop-Dee-Loup/actions/runs?head_sha=8533bae6a38c43c56ed42ad7a33200ec33696b4b` shows a completed, successful run of `control-plane-paths.yml` (the PR's actual final head that was squash-merged into the commit above) — not merely that file contents look correct.
8. Confirm `node tools/check-control-plane-paths.mjs` and `node tools/check-startup-budget.mjs` both report OK at the merge commit, and that no other orchestration exit-code contract (e.g. `DISPATCH_MANIFEST_VERIFIED` / `READY_TO_DISPATCH_UNITS` on an already-valid manifest) regressed as a side effect of these four fixes.
